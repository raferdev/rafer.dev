---
title: Introduction
description: Why the filesystem is the source of truth.
---

This folder sorts by numeric prefix, so the order you see in the sidebar is the
order on disk. Renaming `01-` to `03-` moves the page.

## Prefixes are stripped

The prefix orders the file but never reaches the URL. `01-introduction.mdx`
serves at `/getting-started/introduction`.
