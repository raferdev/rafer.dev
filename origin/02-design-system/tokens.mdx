---
title: Tokens
description: Colour, radius and typography variables.
---

Tokens live in `packages/ui/src/styles/globals.css` and are consumed through
semantic class names.

## Semantic colours

Use `bg-background`, `text-muted-foreground` and friends rather than raw values,
so both themes resolve without a `dark:` override.
